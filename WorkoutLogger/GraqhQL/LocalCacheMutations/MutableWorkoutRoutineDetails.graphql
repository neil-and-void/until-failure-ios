fragment MutableWorkoutRoutineDetails on WorkoutRoutine @apollo_client_ios_localCacheMutation {
    id
    name
    active
    exerciseRoutines {
        id
        active
        name
        sets
        reps
    }
}
